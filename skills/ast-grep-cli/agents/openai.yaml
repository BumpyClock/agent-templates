interface:
  display_name: "ast-grep CLI"
  short_description: "Find code by AST shape or relationship"
  default_prompt: "Use $ast-grep-cli to run a scoped structural code search, developing a rule only when the query needs one."
