interface:
  display_name: "Maintain Verification Skill"
  short_description: "Audit and refresh a project's verification skill"
  default_prompt: "Use $maintain-verification-skill to audit this project's verification skill and correct proven drift."
policy:
  allow_implicit_invocation: false
