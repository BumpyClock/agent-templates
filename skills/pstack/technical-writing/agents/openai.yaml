interface:
  display_name: "Technical Writing"
  short_description: "Check technical accuracy, terminology, and reader context"
  default_prompt: "Use $technical-writing to write or review technical documents, PR descriptions, or commit messages. Check accuracy, terminology, reader context, and evidence while preserving the requested scope and format."
policy:
  allow_implicit_invocation: false
