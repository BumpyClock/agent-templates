interface:
  display_name: "Unslop"
  short_description: "Cut AI tells from writing and add human voice"
  default_prompt: "Use $unslop to cut AI writing patterns and add human voice."
policy:
  allow_implicit_invocation: true
