interface:
  display_name: "Unslop"
  short_description: "Remove filler and repetition without factual drift"
  default_prompt: "Use $unslop to remove filler and repetition while preserving factual precision, source material, and the document's intended voice."
policy:
  allow_implicit_invocation: true
