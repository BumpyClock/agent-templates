interface:
  display_name: "Recall"
  short_description: "Rebuild recent work context and next steps"
  default_prompt: "Use $recall to reconstruct my recent work on this topic and return a concise current-state brief."
policy:
  allow_implicit_invocation: false
