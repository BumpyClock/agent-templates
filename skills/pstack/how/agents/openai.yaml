interface:
  display_name: "How"
  short_description: "Explain how a subsystem works, where it lives, and who owns it"
  default_prompt: "Use $how to explain how this subsystem works, where it lives, and which package owns it."
policy:
  allow_implicit_invocation: true
