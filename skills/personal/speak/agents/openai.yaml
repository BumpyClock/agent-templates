interface:
  display_name: "Speak"
  short_description: "Generate a voice note or play a scoped alert"
  default_prompt: "Use $speak to generate a file-only voice note or play an audible notification, matching the authorized delivery mode and message content."
policy:
  allow_implicit_invocation: false
