interface:
  display_name: "Improve Animations"
  short_description: "Audit motion systems and plan animation improvements"
  default_prompt: "Use $improve-animations to audit the requested motion scope read-only and report evidence-backed findings. Create plans or apply findings only when requested."
