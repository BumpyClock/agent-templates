interface:
  display_name: "Handoff"
  short_description: "Compact a conversation into a handoff"
  default_prompt: "Use $handoff to compact this conversation into a focused temporary handoff document for the next agent session."
policy:
  allow_implicit_invocation: false
