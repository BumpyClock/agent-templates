interface:
  display_name: "To Spec"
  short_description: "Turn a conversation into a spec"
  default_prompt: "Use $to-spec to synthesize the current conversation into a project-ready specification without another interview."
policy:
  allow_implicit_invocation: false
