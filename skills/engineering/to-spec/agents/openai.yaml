interface:
  display_name: "To Spec"
  short_description: "Turn a conversation into a spec"
  default_prompt: "Use $to-spec to synthesize settled context into a scoped specification without another interview, publishing to the configured destination only when requested."
policy:
  allow_implicit_invocation: false
