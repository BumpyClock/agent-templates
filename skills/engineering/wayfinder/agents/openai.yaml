interface:
  display_name: "Wayfinder"
  short_description: "Map a large effort as decision tickets"
  default_prompt: "Use $wayfinder to chart this large effort as decision tickets, or work the specified map within the authorized scope."
policy:
  allow_implicit_invocation: false
