interface:
  display_name: "Triage"
  short_description: "Move issues through triage roles"
  default_prompt: "Use $triage to categorize, verify, and advance this issue or external pull request through the configured triage state machine."
policy:
  allow_implicit_invocation: false
