interface:
  display_name: "To Tickets"
  short_description: "Split a plan into tracer-bullet tickets"
  default_prompt: "Use $to-tickets to split this plan, specification, or conversation into blocked tracer-bullet tickets for the configured tracker."
policy:
  allow_implicit_invocation: false
