interface:
  display_name: "To Tickets"
  short_description: "Split a plan into tracer-bullet tickets"
  default_prompt: "Use $to-tickets to split this plan, specification, or conversation into dependency-linked tracer-bullet tickets, reusing unchanged approvals and publishing only when authorized."
policy:
  allow_implicit_invocation: false
