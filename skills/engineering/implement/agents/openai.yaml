interface:
  display_name: "Implement"
  short_description: "Build work from a spec or tickets"
  default_prompt: "Use $implement to deliver this spec or ticket set with targeted checks and test-driven development where appropriate."
policy:
  allow_implicit_invocation: false
