interface:
  display_name: "Reflect"
  short_description: "Turn transcript learnings into durable skill edits"
  default_prompt: "Use $reflect to review this conversation for durable lessons and apply them to existing skills."
policy:
  allow_implicit_invocation: false
